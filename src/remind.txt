document.write()  能执行，但在计时器里无法判断是否是js语句，每句代码还未执行完就会打到屏幕上
window.onload()
-----------------
const newScript = document.createElement('script');
newScript.innerHTML = string.substr(0, n);
document.appendChild(newScript);

document.removeChild(newScript);
-----------------
document.innerHTML()
document.outerHTML()